windowrulev2 = suppressevent maximize, class:.*
# windowrulev2 = float, class:^(file_progress|confirm|dialog|download|notification|page-info|splash|toolbar|utility|hyprpolkit)$

# --- 모든창을 플로팅
# windowrule = float, modal:true
# windowrule = float, class:.*
# 잘못된 렌더링을 방지하기 위해 필요한 창만 타일링 전환 설정하는것이 편할수도 있습니다.

windowrule = float, class:^(floating_foot)$
windowrulev2 = opacity 1.0 override,title:.*- YouTube.*
windowrulev2 = opacity 1.0 override, workspace name:PIP silent, float, pin, keepaspectratio, size 30% 30%, move onscreen 68% 68%, title:.*PIP.*
windowrulev2 = float, opacity 1.0 override, keepaspectratio, class:^(mpv)$

# --- google-chrome
windowrulev2 = float, class:google-chrome
windowrulev2 = tile, class:google-chrome
# - 이 크롬설정은 일부러 이렇게 한것입니다.
# (사이즈가 고정된 팝업창이 플로팅 되는 경우 잘못된 렌더링을 막기위해.)
windowrulev2 = float, initialTitle:.*about:blank.*
windowrulev2 = float, initialTitle:.*지니 - Chrome.*
# windowrulev2 = float, initialTitle:(.*about:blank.*|.*예시 - Chrome.*)

windowrulev2 = float, class:^(Steam)$, class:^(steamwebhelper)$
windowrulev2 = workspace 2 silent, class:^(discord)$

